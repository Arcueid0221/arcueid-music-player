# 后续指导方针

## 设计与架构原则

1. `player-element.ts` 只做属性/方法适配，不再放歌单数据、DOM 拼装或音频事件。
2. 播放行为先写在 `core/`，视觉行为写在 `ui/`；网络与持久化统一放进 `services/`。
3. 状态只有一个来源：界面从 store 渲染，不直接猜测 `<audio>` 的状态。
4. 新功能优先增加小而可替换的服务，不把云端 API、歌词解析、缓存和 UI 写在同一文件。
5. 每个纯逻辑模块都配单元测试；播放器主路径用浏览器交互测试覆盖。

## 功能阶段

### P1：把播放器做稳

- 为 AudioEngine 增加媒体会话（Media Session）与系统快捷键。
- 增加缓冲进度、网络失败重试、无效音频跳过与更明确的错误态。
- 给 Controller 补齐基于 mock engine 的状态机测试。
- 增加移动端拖动进度与完整键盘可访问性测试。

### P2：接近参考项目的实用能力

- 抽象 `PlaylistProvider`，并行支持本地数组、JSON API 与用户选择文件。
- 增加主题 token、深色主题与跟随系统主题，不让业务组件感知具体颜色。
- 增加播放队列增删、排序、搜索和当前歌曲自动定位。
- 支持逐字歌词、歌词偏移校准与无歌词降级。

### P3：面向复用与发布

- 增加实例化控制器，支持自定义挂载点、生命周期钩子和多实例隔离。
- 设计稳定的事件 API：`trackchange`、`playbackchange`、`error`。
- 产出 ESM/IIFE、类型声明、最小接入示例和版本迁移说明。
- 在发布前补齐跨域音频、SSR 动态加载和浏览器兼容策略。

## 明确延后

- 云端账号、收藏同步、推荐算法与数据库不应进入播放器内核。
- 樱花等页面级特效做成独立插件，不与播放生命周期绑定。
- 在基础播放链路稳定前，不引入大型框架或完整状态管理库。
